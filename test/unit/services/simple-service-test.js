// Simple service test to verify Jest test discovery in subdirectories

describe('Simple Service Test', () => {
  it('should pass a simple service test', () => {
    console.log('Running simple service test...');
    expect(true).toBe(true);
  });
});
