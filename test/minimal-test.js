// Minimal test to verify Jest is working
console.log('=== Starting Minimal Test (JavaScript) ===');

test('should pass a simple assertion', () => {
  console.log('Running minimal test');
  expect(1 + 1).toBe(2);
});
